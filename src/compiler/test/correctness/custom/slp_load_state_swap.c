// expect: 4

#include <stdint.h>

typedef struct {
	double x, y;
} SD;

double G;

uint64_t swap(uint64_t a, float f) {
	double d[2] = {0, 0};
	for(int k = 0; k < 2; k++)
		;
	double v0 = 2 - 2 > -1e15 && 2 < 1e15 ? f : 5, v4 = a;
	SD u = (SD){0, 0}, v = {v4};
	for(int i = 0; i < 2; i++) {
		for(; 1 ? f : 5;)
			;
		d[1] += 9;
		if(G)
			;
		SD t = u;
		u = v;
		v = t;
	}
	return v.x;
}

int main(void) {
	return (int)swap(4, 0);
}
